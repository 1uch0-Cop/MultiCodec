{
  "name": "multicodec",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "Motor web educativo de MultiCodec",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check web/multicodec.js && node --check web/app.js",
    "test": "node --test tests/test_web.mjs",
    "test:web": "node --test tests/test_web.mjs"
  }
}
